{
  "name": "relayagent",
  "version": "0.3.11",
  "description": "A personal substrate for agent packages that ship their own screens.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "homepage": "https://github.com/relayax/relayagent",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/relayax/relayagent.git"
  },
  "bugs": {
    "url": "https://github.com/relayax/relayagent/issues"
  },
  "keywords": [
    "agent",
    "ai-agent",
    "agent-package",
    "claude-code",
    "codex",
    "harness",
    "manifest",
    "local-first"
  ],
  "engines": {
    "node": ">=22.6"
  },
  "bin": {
    "relay": "./runner/cli.ts"
  },
  "scripts": {
    "relay": "node --experimental-strip-types runner/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test runner/**/*.test.ts",
    "validate": "node --experimental-strip-types tools/validate-all.ts",
    "build:widget": "npm --prefix chat install --silent && npm --prefix chat run build:chat",
    "typecheck:widget": "npm --prefix chat install --silent && npm --prefix chat run typecheck",
    "test:widget": "npm --prefix chat install --silent && npm --prefix chat run test",
    "pack:contract": "node tools/pack-contract.mjs"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
